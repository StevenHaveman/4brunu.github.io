sudo: false
language: ruby
cache: bundler
rvm:
- 2.3.3

install:
  # Install Jekyll and dependencies
  - bundle install

script: bundle exec rake site:deploy #--quiet #--trace

# Generate your secure token with the travis gem:
# get Github token from your Travis CI profile page
# gem install travis
# GH_TOKEN from https://github.com/settings/tokens
# travis encrypt 'GIT_NAME="STEVENHAVEMAN" GIT_EMAIL="STEVENHAVEMAN@GMAIL.COM" GH_TOKEN=e4dd871925ee6ebea952ddb0eda11d0ee7e4e195'

env:
  global:
    - secure: #to be added

branches:
  only:
    - source
